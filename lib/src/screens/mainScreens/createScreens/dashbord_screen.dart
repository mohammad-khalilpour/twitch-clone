import 'package:flutter/cupertino.dart';

class DashordScreen extends StatelessWidget {
  const DashordScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}